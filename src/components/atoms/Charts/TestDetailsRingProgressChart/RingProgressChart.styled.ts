import {RingProgress} from '@ant-design/charts';

import styled from 'styled-components';

export const StyledRingProgressChart = styled(RingProgress)`
  .g2-html-annotation {
    top: 50% !important;
    left: 50% !important;

    width: unset !important;
  }
`;
