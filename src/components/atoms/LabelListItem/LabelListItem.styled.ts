import styled from 'styled-components';

import Colors from '@styles/Colors';

export const StyledLabelListItem = styled.li`
  padding: 0px 10px;
  border: 1px solid ${Colors.purple};
  border-radius: 12px;

  color: ${Colors.purple};
  background: #151515;

  font-size: 14px;
  font-weight: 400;
`;
